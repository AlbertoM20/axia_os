[project]
name = "axia-engine"
version = "0.1.0"
description = "Motor económico y de riesgo de AXIA"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
